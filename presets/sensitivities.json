{
  "id": "sensitivities", "label": "Sensitivities", "icon": "fa-heart-pulse",
  "source": "preset:sensitivities",
  "appliesToRoles": ["protagonist","npc"],
  "autoUpdate": true,
  "injection": {
    "enabled": true,
    "trigger": "tag",
    "tags": ["sexual"],
    "position": "in-chat",
    "depth": 2,
    "template": "<reference_injection>\n**{{subject}}'s sensitivities (INTERNAL REFERENCE ONLY — 100% = baseline, higher = more sensitive, lower = less sensitive. Use these to calibrate how strongly {{subject}} reacts during intimate scenes, but NEVER state the numbers directly or otherwise surface this list in the prose):**\n{{fields}}\n</reference_injection>"
  },
  "fields": [
    { "id": "breasts", "label": "Breasts", "type": "number", "min": 0, "default": 100, "allowDelta": true, "describable": false,
      "inclusion": { "rule": "tag", "tags": ["sexual"] },
      "injection": { "enabled": true }, "displayAs": "percent" },
    { "id": "anus", "label": "Anus", "type": "number", "min": 0, "default": 100, "allowDelta": true, "describable": false,
      "inclusion": { "rule": "tag", "tags": ["sexual"] },
      "injection": { "enabled": true }, "displayAs": "percent" },
    { "id": "vagina", "label": "Vagina", "type": "number", "min": 0, "default": 100, "allowDelta": true, "describable": false,
      "inclusion": { "rule": "tag", "tags": ["sexual"] },
      "injection": { "enabled": true }, "displayAs": "percent" }
  ]
}
